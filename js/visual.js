class VisualMode {

}
class Fragment {
    // parts of this fragment: {str:String, type: String, err: bool}
    parts;

    constructor(input, type) {
        if(type === 'text') {
            this.init_from_string(input, 0, '');
        }
        else {

        }
    }
    init_from_string(str, j, tag) {
        let start = output.length - 1;
        while(true) {
            let tag = null;
            
        }
    }
}
